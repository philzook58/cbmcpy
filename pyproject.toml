[project]
name = "cbmc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.ty.src]
include = ["src", "tests"]
exclude = ["cbmc"]

[tool.ruff]
include = ["src/**/*.py", "tests/**/*.py"]
exclude = ["cbmc"]
